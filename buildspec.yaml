version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo start
      - apt-get install sshpass
      - sshpass -p trolololo ssh pi@178.48.46.148 -p 5554
      - touch touch.txt
artifacts:
  files:
    - '**/*'

cache:
  paths:
    - '/root/.npm/**/*'